# Example environment configuration (not used by docker-compose)
# These values mirror the hardcoded settings in docker-compose.yml and src/database.js.

MONGO_HOST=mongo
MONGO_PORT=27017
MONGO_DB=utkrusht_store
MONGO_USER=appuser
MONGO_PASSWORD=apppassword

API_PORT=3000
